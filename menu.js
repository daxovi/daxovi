const projects = document.getElementsByClassName("projects")[0];

/*
function toggleMenu() {
    projects.classList.toggle("active");
}
*/

const toggleMenu = () => projects.classList.toggle("active");